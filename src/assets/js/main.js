// @main
// Luis Matute
// Feb-15

// Common Dependencies ==========================
	var config = require('./config');
	window.jQuery = window.$ = require('jquery');
	window.app = {
		config: config
	};